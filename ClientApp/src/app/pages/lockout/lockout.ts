import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-lockout',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './lockout.html',
  styleUrl: './lockout.css',
})
export class LockoutComponent {}
